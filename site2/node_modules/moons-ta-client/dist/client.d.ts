import { CustomEventEmitter } from "./custom-event-emitter.js";
import { Packet } from "./models/packets.js";
export type ClientEvents = {
    packetReceived: Packet;
    connectedToServer: {};
    failedToConnectToServer: {};
    disconnectedFromServer: {};
};
export declare class Client extends CustomEventEmitter<ClientEvents> {
    private address;
    private port;
    private token;
    private websocket;
    private websocketWasConnected;
    constructor(address: string, port: string, token?: string);
    get isConnected(): boolean;
    get readyState(): 0 | 2 | 1 | 3 | undefined;
    connect(): void;
    disconnect(): void;
    setToken(token: string): void;
    send(packet: Packet, ids?: string[]): void;
}
